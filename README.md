# amazon-clickstream-data
This repository contains the logic for pushing clickstream data from Amazon S3 to various cloud platforms such as Azure, GCP, and AWS. It replicates a real-life use case where data is fetched incrementally from S3 and stored in the cloud.
